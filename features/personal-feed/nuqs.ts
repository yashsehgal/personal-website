export const NUQS_FEED_SESSION: string = "session" as const;

/** Active root message id for the reply-in-thread side panel (`?thread=…`). Only one thread at a time. */
export const NUQS_FEED_MESSAGE_THREAD: string = "thread" as const;
