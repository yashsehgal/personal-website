import MDXHeader from '@/components/main/MDXHeader';
import ViewContainer from '@/components/layout/ViewContainer';
import Section from '@/components/layout/Section';
import CraftedAccordion from '@/components/crafts/CraftedAccordion';

<MDXHeader />

<ViewContainer className="mdx-content">
  <Section className="component-display-wrapper">
    <div className="grid grid-cols-1 gap-4">
      <CraftedAccordion trigger={'Is this accordion accessible'}>
        {
          'Yes! This accordion component is totally accessible, also for screen-readers.'
        }
      </CraftedAccordion>
      <CraftedAccordion trigger={'What functionalities are added?'}>
        {'One accordion open at a time. Responsive, for all breakpoints'}
      </CraftedAccordion>
      <CraftedAccordion trigger={'What else?'}>
        {'Has an onclick action sound'}
      </CraftedAccordion>
    </div>
  </Section>
</ViewContainer>
